apiVersion: v1
kind: Service
metadata:
  name: skipa-llm
  namespace: skala3-finalproj-class2-team8
spec:
  type: ClusterIP
  selector:
    app: skipa-llm
  ports:
    - name: http
      port: 8000
      targetPort: 8000
