apiVersion: v1
kind: Service
metadata:
  name: skipa-embedding
  namespace: skala3-finalproj-class2-team8
spec:
  type: ClusterIP
  selector:
    app: skipa-embedding
  ports:
    - name: http
      port: 8001
      targetPort: 8001
